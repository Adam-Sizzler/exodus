# Subscription Frontend

Placeholder directory for the subscription web UI.

If you add a Vite/React app here, Docker will build it and serve the
compiled files from `/app/ui` in the subscription container.
