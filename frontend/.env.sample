# Frontend-only variables.
# Copy to frontend/.env only if you need local override.
# Otherwise configure these in project root .env.

DOMAIN_BACKEND=http://127.0.0.1:9242
DOMAIN_OVERRIDE=1
NODE_ENV=development

CROWDIN=0
# CROWDIN_API_TOKEN=
# CROWDIN_PROJECT_ID=
