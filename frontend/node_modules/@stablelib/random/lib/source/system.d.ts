import type { RandomSource } from "@stablelib/random/lib/source/index.js";
export declare class SystemRandomSource implements RandomSource {
    isAvailable: boolean;
    isInstantiated: boolean;
    constructor();
    randomBytes(length: number): Uint8Array;
}
