'use client';
import { useMatches } from '@mantine/core';

function useSplitResizerOrientation(orientation) {
  const responsiveOrientation = useMatches(typeof orientation === "string" ? {} : orientation);
  if (typeof orientation === "string") {
    return orientation;
  }
  return responsiveOrientation;
}

export { useSplitResizerOrientation };
//# sourceMappingURL=use-split-resizer-orientation.mjs.map
