'use client';
'use strict';

var core = require('@mantine/core');

function useSplitResizerOrientation(orientation) {
  const responsiveOrientation = core.useMatches(typeof orientation === "string" ? {} : orientation);
  if (typeof orientation === "string") {
    return orientation;
  }
  return responsiveOrientation;
}

exports.useSplitResizerOrientation = useSplitResizerOrientation;
//# sourceMappingURL=use-split-resizer-orientation.cjs.map
