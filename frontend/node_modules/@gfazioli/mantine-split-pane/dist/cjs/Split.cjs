'use client';
'use strict';

var React = require('react');
var core = require('@mantine/core');
var SplitDynamic = require('./Dynamic/SplitDynamic.cjs');
var useSplitResizerOrientation = require('./hooks/use-split-resizer-orientation.cjs');
var SplitPane = require('./Pane/SplitPane.cjs');
var SplitResizer = require('./Resizer/SplitResizer.cjs');
var Split_context = require('./Split.context.cjs');
var Split_module = require('./Split.module.css.cjs');

const defaultProps = {
  inline: false,
  autoResizers: false,
  ...SplitResizer.defaultProps
};
const varsResolver = core.createVarsResolver((_, { inline }) => ({
  root: {
    "--split-inline": inline ? "inline-flex" : "flex"
  }
}));
const Split = core.factory((_props, ref) => {
  const props = core.useProps("Split", defaultProps, _props);
  const {
    inline,
    autoResizers,
    orientation: propOrientation,
    opacity,
    size,
    radius,
    withKnob,
    knobAlwaysOn,
    knobSize,
    knobOpacity,
    knobRadius,
    knobColor,
    knobHoverColor,
    spacing,
    step,
    shiftStep,
    cursorVertical,
    cursorHorizontal,
    color,
    hoverColor,
    variant,
    gradient,
    hoverGradient,
    classNames,
    style,
    styles,
    unstyled,
    vars,
    mod,
    children,
    className,
    ...others
  } = props;
  const orientation = useSplitResizerOrientation.useSplitResizerOrientation(propOrientation);
  const getStyles = core.useStyles({
    name: "Split",
    props,
    classes: Split_module,
    className,
    style,
    classNames,
    styles,
    unstyled,
    vars,
    varsResolver
  });
  const childrenCount = React.Children.count(children);
  const childRefs = React.useMemo(
    () => Array.from({ length: childrenCount }, () => React.createRef()),
    [childrenCount]
  );
  let clonedChildren;
  if (autoResizers) {
    const elementsWithResizers = [];
    let resizerIndex = 0;
    let paneIndex = 0;
    const paneRefs = [];
    React.Children.forEach(children, (child, index) => {
      if (React.isValidElement(child) && child.type === SplitPane.SplitPane) {
        const ref2 = childRefs[index];
        paneRefs.push(ref2);
        elementsWithResizers.push(
          React.cloneElement(child, {
            key: child.key || `pane-${paneIndex}`,
            ref: ref2
          })
        );
        paneIndex++;
      }
    });
    const finalElements = [];
    elementsWithResizers.forEach((pane, index) => {
      finalElements.push(pane);
      if (index < elementsWithResizers.length - 1) {
        const beforeRef = paneRefs[index];
        const afterRef = paneRefs[index + 1];
        finalElements.push(
          /* @__PURE__ */ React.createElement(
            SplitResizer.SplitResizer,
            {
              key: `auto-resizer-${resizerIndex++}`,
              __beforeRef: beforeRef,
              __afterRef: afterRef
            }
          )
        );
      }
    });
    clonedChildren = finalElements;
  } else {
    clonedChildren = React.Children.map(children, (child, index) => {
      if (React.isValidElement(child)) {
        if (child.type === SplitResizer.SplitResizer) {
          const beforeRef = childRefs[index - 1];
          const afterRef = childRefs[index + 1];
          return React.cloneElement(child, {
            __beforeRef: beforeRef,
            __afterRef: afterRef
          });
        } else if (child.type === SplitPane.SplitPane) {
          return React.cloneElement(child, {
            ref: childRefs[index]
          });
        }
        return child;
      }
      return child;
    });
  }
  return /* @__PURE__ */ React.createElement(
    Split_context.SplitContextProvider,
    {
      value: {
        orientation,
        size,
        opacity,
        radius,
        color,
        hoverColor,
        knobSize,
        knobOpacity,
        knobRadius,
        knobColor,
        knobHoverColor,
        variant,
        withKnob,
        knobAlwaysOn,
        spacing,
        step,
        shiftStep,
        cursorVertical,
        cursorHorizontal,
        gradient,
        hoverGradient
      }
    },
    /* @__PURE__ */ React.createElement(core.Box, { ref, mod: { orientation }, ...getStyles("root"), ...others }, clonedChildren)
  );
});
Split.classes = Split_module;
Split.displayName = "Split";
Split.Pane = SplitPane.SplitPane;
Split.Resizer = SplitResizer.SplitResizer;
Split.Dynamic = SplitDynamic.SplitDynamic;

exports.Split = Split;
//# sourceMappingURL=Split.cjs.map
